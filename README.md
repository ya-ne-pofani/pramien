# Pramien - Flask Chat Application

Современное веб-приложение для чата с поддержкой групп, личных сообщений и администрирования.

## Особенности

- 💬 Реального времени чат через WebSocket (Socket.IO)
- 👥 Групповые и личные сообщения
- 🔐 Система аутентификации и авторизации
- 👮 Административная панель
- 🏷️ Система тегов и бейджей для пользователей
- 🚫 Система банов
- 🎨 Настраиваемые аватары (цвет + эмодзи)
- 📝 Профили пользователей

## Требования

- Python 3.7+
- SQLite3
- Flask
- Flask-SocketIO

## Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/ya-ne-pofani/pramien.git
cd pramien
```

2. Установите зависимости:
```bash
pip install flask flask-socketio werkzeug markupsafe
```

3. Настройте переменные окружения:
```bash
# Создайте .env файл или установите переменные окружения
export SECRET_KEY=$(python3 -c "import secrets; print(secrets.token_urlsafe(32))")
export CORS_ORIGINS=http://localhost:5000
export FLASK_DEBUG=false
```

4. Запустите приложение:
```bash
python app.py
```

5. Откройте браузер и перейдите по адресу `http://localhost:5000`

## Структура проекта

```
pramien/
├── app.py              # Основное приложение Flask
├── instance/
│   └── chat.db         # База данных SQLite (создается автоматически)
├── static/
│   ├── css/           # Стили
│   │   ├── admin.css
│   │   ├── auth.css
│   │   └── style.css
│   └── js/            # JavaScript
│       ├── admin.js
│       ├── auth.js
│       └── chat.js
├── templates/         # HTML шаблоны
│   ├── admin.html
│   ├── admin_login.html
│   ├── banned.html
│   ├── chat.html
│   ├── login.html
│   └── register.html
├── SECURITY.md        # Документация по безопасности
└── README.md          # Этот файл
```

## Использование

### Регистрация

1. Перейдите на `/register`
2. Заполните форму регистрации (username, password, nickname, handle)
3. После регистрации войдите через `/login`

### Чат

- **Общий чат**: Доступен всем пользователям через комнату `#Global`
- **Личные сообщения**: Нажмите на пользователя в списке контактов
- **Группы**: Создавайте и управляйте группами через интерфейс

### Администрирование

1. Первый зарегистрированный админ получает полные права
2. Перейдите на `/admin` для доступа к админ-панели
3. Функции админа:
   - Просмотр статистики
   - Управление пользователями
   - Система банов
   - Назначение тегов

## Безопасность

Приложение включает несколько уровней защиты:

- 🔒 Хеширование паролей (werkzeug.security)
- 🛡️ Защита от XSS (экранирование HTML)
- 🔐 Безопасные сессии (SECRET_KEY)
- 🚫 Проверка банов перед каждым запросом
- ✅ Валидация входных данных

**Важно**: Обязательно прочитайте [SECURITY.md](SECURITY.md) перед развертыванием в production!

## Настройка переменных окружения

| Переменная | Обязательная | Описание | Пример |
|------------|--------------|----------|--------|
| `SECRET_KEY` | Да | Секретный ключ для сессий | Случайная строка 32+ символов |
| `CORS_ORIGINS` | Да | Разрешенные CORS домены | `https://domain.com,https://www.domain.com` |
| `FLASK_DEBUG` | Нет | Режим отладки (по умолчанию False) | `false` или `true` |

## Развертывание в production

1. **Никогда не используйте встроенный сервер Flask в production!**
2. Используйте WSGI сервер (например, Gunicorn):
```bash
pip install gunicorn eventlet
gunicorn --worker-class eventlet -w 1 --bind 0.0.0.0:5000 app:app
```

3. Настройте обратный прокси (Nginx):
```nginx
location / {
    proxy_pass http://127.0.0.1:5000;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
}
```

4. Обязательно:
   - Установите сильный `SECRET_KEY`
   - Ограничьте `CORS_ORIGINS`
   - Используйте HTTPS
   - Настройте регулярные бэкапы БД
   - Мониторьте логи

## API Endpoints

### Аутентификация
- `POST /api/auth/login` - Вход
- `POST /api/auth/logout` - Выход
- `POST /api/auth/register` - Регистрация

### Пользователи
- `GET /api/chats` - Получить список чатов
- `GET /api/profile/<username>` - Профиль пользователя
- `POST /api/user/profile` - Обновить профиль

### Администрирование
- `GET /api/admin/stats` - Статистика
- `GET /api/admin/users` - Список пользователей
- `POST /api/admin/ban` - Забанить пользователя
- `POST /api/admin/unban` - Разбанить пользователя
- `GET /api/admin/tags/list` - Список тегов
- `POST /api/admin/tags/assign` - Назначить тег

### WebSocket Events
- `join` - Присоединиться к комнате
- `send_message` - Отправить сообщение
- `request_history` - Запросить историю
- `typing_event` - Событие печати

## Разработка

### База данных

Схема БД создается автоматически при первом запуске:
- `users` - Пользователи
- `messages` - Сообщения
- `groups` - Группы
- `group_members` - Члены групп
- `admins` - Администраторы
- `bans` - Баны
- `tags` - Теги
- `user_tags` - Связь пользователей и тегов

### Добавление новых функций

1. Добавьте роут в `app.py`
2. Создайте или обновите HTML шаблон
3. Добавьте стили в соответствующий CSS файл
4. Добавьте JS логику в соответствующий JS файл

## Лицензия

[Укажите вашу лицензию]

## Поддержка

Если нашли баг или хотите предложить улучшение, создайте Issue на GitHub.

Для вопросов безопасности см. [SECURITY.md](SECURITY.md).
